# Artemis Configuration
# Copy this file to .env and fill in your actual values

# LLM API Keys (at least one required)
ANTHROPIC_API_KEY=sk-ant-your-key-here
OPENAI_API_KEY=sk-your-key-here

# HuggingFace (optional, for downloading code examples)
HF_TOKEN=hf_your-token-here

# Neo4j Knowledge Graph (optional)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-password-here

# Artemis Paths (usually auto-configured)
ARTEMIS_DATA_DIR=../.artemis_data
ARTEMIS_DB_DIR=./db
ARTEMIS_OUTPUT_DIR=./outputs

# Logging
LOG_LEVEL=INFO
DEBUG_MODE=false

# Code Generation & Validation (Layer 3.6: Streaming Validation)
# Enable real-time validation during LLM code generation
# WHY: Stops hallucinations DURING generation (not after), saves tokens and time
# Options: true, false (default: false)
ARTEMIS_ENABLE_STREAMING_VALIDATION=false

# Retry with Refinement (Layer 4: Automated Retry)
# Maximum retry attempts with prompt refinement on validation failure
# WHY: Automatically fixes validation failures instead of giving up
# Options: 1-5 (default: 3)
ARTEMIS_MAX_VALIDATION_RETRIES=3

# Minimum confidence score to accept generated code (0.0-1.0)
# WHY: Ensures code meets quality threshold across all validation layers
# Options: 0.0-1.0 (default: 0.85)
ARTEMIS_CONFIDENCE_THRESHOLD=0.85

# Development
PYTHONPATH=.
